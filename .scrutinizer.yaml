checks:
    php: true

coding_style:
    php:
        spaces:
            before_parentheses:
                closure_definition: true
            around_operators:
                concatenation: true

build:
    nodes:
        analysis:
            image: default-bionic
            environment:
                php: 8.1
            tests:
                override:
                    - php-scrutinizer-run
